
all:

strip:

install:
#	mkdir -p $(DESTDIR)$(datadir)/applications
#	install mp.desktop $(DESTDIR)$(datadir)/applications
	mkdir -p $(DESTDIR)$(datadir)/pixmaps
	install mp.xpm $(DESTDIR)$(datadir)/pixmaps
	mkdir -p $(DESTDIR)$(mandir)/man1
	install mp.1 $(DESTDIR)$(mandir)/man1
	mkdir -p $(DESTDIR)$(sysconfdir)
	install mprc.sample $(DESTDIR)$(sysconfdir)

install-strip: install

uninstall:
#	rm -f $(DESTDIR)$(datadir)/applications/mp.desktop
	rm -f $(DESTDIR)$(datadir)/pixmaps/mp.xpm
	rm -f $(DESTDIR)$(mandir)/man1/mp1.1
	rm -f $(DESTDIR)$(sysconfdir)/mprc.sample

clean:

distclean:
	rm -f Makefile
